// Facade re-export — not yet migrated. Base UI ships no Button primitive, so
// Button will become a styled native <button> (Tier B) with our variants later.
// Until then base-ui/Button IS ui/Button, so the look is already identical.
export * from "@workspace/ui/components/Button"
