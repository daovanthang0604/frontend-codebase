// Spinner is a pure presentational SVG (no react-aria/Radix to rebuild on Base
// UI), so base-ui re-exports ui's Spinner verbatim rather than duplicating it —
// same strangler facade as Button.
export { Spinner } from "@workspace/ui/components/Spinner"
