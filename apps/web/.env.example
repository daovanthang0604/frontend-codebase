# Base URL for the backend API (consumed by src/services/api.ts).
# No backend ships with this template — point it at your own API.
# Copy this file to `.env.local` and adjust.
VITE_BASE_API=http://localhost:5001
